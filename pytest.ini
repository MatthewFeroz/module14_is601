[pytest]
testpaths = tests
python_files = test_*.py
addopts =
    --strict-markers
    --cov=app
    --cov-report=term-missing
    --cov-report=xml:coverage.xml
markers =
    e2e: real-browser end-to-end tests
